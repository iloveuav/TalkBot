import Base, { IPluginBaseConfig } from '../base';
interface ZoomSliderConfig extends IPluginBaseConfig {
    minZoom: number;
    maxZoom: number;
    sliderWidthPercent: number;
    dragTimestamp: number;
    sliderPosition: 'bottom' | 'top';
    customizedStyle: string;
}
/**
 * 滑块缩放工具
 */
export default class ZoomSlider extends Base {
    constructor(props?: ZoomSliderConfig);
    getDefaultCfgs(): {
        minZoom: number;
        maxZoom: number;
        sliderWidthPercent: number;
        dragTimestamp: number;
        sliderPosition: string;
        customizedStyle: string;
        getContent: () => string;
        getCss: () => string;
    };
    getEvents(): {
        dragstart: string;
        dragend: string;
        pinchmove: string;
        pinchend: string;
    };
    init(): void;
    /**
     * 初始化滑块视图
     */
    initSlider(): void;
    /**
     * 开始拖动画布
     */
    onDragStart(): void;
    /**
     * 停止拖动画布
     */
    onDragEnd(): Promise<void>;
    /**
     * 更新滑块拖动的位置
     */
    updateHandlePosition(distance: any): void;
    /**
     * 更新画布缩放
     */
    updateScale(scaleRatio: any): void;
}
export {};
