import { SimpleTimeBarOption, TickTimeBarOption, TrendTimeBarOption } from '../type/time-bar';
/**
 * 创建简单模式的时间轴
 */
export declare function createSimpleTimeBarUI({ width, padding, group, data, slider, controllerCfg, sliderTick, }: SimpleTimeBarOption): any;
/**
 * 创建刻度模式时间轴
 */
export declare function createTickUI({ group, data, width, padding, controllerCfg, tick, }: TickTimeBarOption): any;
/**
 * 创建趋势时间轴
 */
export declare function createTrendUI({ width, padding, group, data, slider, controllerCfg, trend, sliderTick, }: TrendTimeBarOption): any;
