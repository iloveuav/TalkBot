"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _f6Ui = require("@antv/f6-ui");

function createControlBtn(which, style) {
  var _a = style.height,
      height = _a === void 0 ? 16 : _a,
      fill = style.fill;
  var len = height / 2;
  var html = '';

  if (which === 'next') {
    html = "\n      <shape  type='path' path='[\n        [\"M\", 0, " + (0 - len) + "],\n        [\"L\", " + len + ", 0],\n        [\"L\", 0, " + len + "],\n        [\"Z\", 0, " + (0 - len) + "],\n        [\"M\", 0, 0],\n        [\"L\", " + (0 - len) + ", " + (0 - len) + "],\n        [\"L\", " + (0 - len) + ", " + len + "],\n        [\"Z\"]\n      ]'/>  \n    ";
  }

  if (which === 'prev') {
    html = "\n      <shape  type='path' path='[\n        [\"M\", 0, " + (0 - len) + "],\n        [\"L\", " + (0 - len) + ", 0],\n        [\"L\", 0, " + (0 + len) + "],\n        [\"L\", 0, " + (0 - len) + "],\n        [\"M\", 0, 0],\n        [\"L\", " + (0 + len) + ", " + (0 - len) + "],\n        [\"L\", " + (0 + len) + ", " + (0 + len) + "],\n        [\"Z\"]\n      ]'/>  \n    ";
  }

  var css = "\n    shape {\n      margin-left: " + len + ";\n      width: " + len + ";\n      background: " + fill + ";\n    }\n  ";
  return (0, _f6Ui.createSegmentNode)(html, css);
}

var _default = createControlBtn;
exports.default = _default;