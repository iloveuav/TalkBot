"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.computeLayout = computeLayout;

var _cssLayout = _interopRequireDefault(require("css-layout"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function computeLayout(styleNode) {
  var stack = [styleNode];

  while (stack.length) {
    var node = stack.pop();
    node.children = node.originChildren.filter(function (child) {
      if (child.style.display !== 'none') {
        stack.push(child);
        return true;
      }

      return false;
    });
  }

  (0, _cssLayout.default)(styleNode);
}